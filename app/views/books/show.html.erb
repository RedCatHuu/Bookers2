<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/user', user: @user %>
      <%= render 'book', book: @book %>
    </div>
    
    <div class="offset-1 col-md-8">
      <h2>Book detail</h2>
      <!--<hr>-->
      <table class="table">
        <tbody>
          <tr>
            <td>
              <%= image_tag @book_show.user.get_profile_image(100, 100) %>
              <%= link_to user_path(@book_show.user.id) do %>
                <%= @book_show.user.name %>
              <% end %>
            </td>
            <td><%= @book_show.title %></td>
            <td><%= @book_show.body %></td>
            <td>
              <%= link_to "Edit", book_path(@book_show.id), class:"btn btn-success btn-sm" %>
            </td>
            <td>
              <%= link_to "Destroy", book_path(@book_show.id), method: :delete, class:"btn btn-danger btn-sm" %>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
      
  </div>
</div>
